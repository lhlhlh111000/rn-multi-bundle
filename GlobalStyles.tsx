import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    Container: {
        flex: 1,
        alignItems:'center',
        justifyContent:'center',
    },
    Item: {
        marginTop: 14,
        padding: -1,
        // backgroundColor: '#325422'
    }
})

export default styles;